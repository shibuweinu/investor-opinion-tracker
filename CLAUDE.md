# Claude / OpenClaw entry point

For this repository, treat `SKILL.md` as the authoritative workflow. Browser access remains in the host session; never pass cookies to the Python process. Follow `references/browser-adapter.md`, save only normalized posts, and run the shared CLI. If MCP is available, configure it using `references/workbuddy.md`.

