# Agent entry point

Read and follow `SKILL.md` for investor-opinion-tracking requests. Use the host Agent's authenticated browser only after the user confirms authorization. Never store or print session credentials. Normalize collected posts to the contract in `references/contracts.md`, then invoke `opinion-tracker analyze-file` for deterministic analysis and report generation.

